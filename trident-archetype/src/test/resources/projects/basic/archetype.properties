# Deliberately different from every value used when the archetype was written: a template that
# only works for the parameters its author tried is not parameterised.
groupId=io.acme.qa
artifactId=billing-suite
version=1.0-SNAPSHOT
package=io.acme.qa.billing
prefix=Billing
